init data -> initialization -> data->export = init_env();
ctrl+D



















Some examples of flags in c_lflag include:

ICANON: Canonical mode (input is processed line-by-line).
ECHO: Echo input characters as they are typed.
ISIG: Enable signal generation for certain key sequences (e.g., Ctrl-C for interrupt).
ECHOCTL: Echo control characters in a readable format (e.g., ^C for Ctrl-C).
